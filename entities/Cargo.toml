#cargo-features = ["codegen-backend"]

[package]
name = "entities"
version = "0.1.0"
edition = "2021"

[lib]
name = "entities"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
sea-orm = "1.1.8"
sha2 = "0.10.8"
